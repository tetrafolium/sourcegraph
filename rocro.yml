---
inspecode:

  global:
    runtime:
      go: 1.14    # (1.13 or later)
      nodejs: 10  # (version 8 or 10)

  tasks:

  tools:

    csscomb:
      # auto-fix: true    # avoid damn up pipeline
      thresholds: {num-issues: 999}
      # UnhandledPromiseRejectionWarning: TypeError: Cannot read property 'first' of null
      # UnhandledPromiseRejectionWarning: [object Object]
